# Video Generation System — credentials
# Copy this file to `.env` (same folder as your render script, or your
# memory root) and replace the placeholder with your real BytePlus
# ModelArk API key.
#
# IMPORTANT: add `.env` to your .gitignore — NEVER commit your real key.

ARK_API_KEY=REPLACE-WITH-YOUR-KEY
