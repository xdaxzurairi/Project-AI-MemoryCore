# Diary — 2026-08-31

## Ringkasan
Hari ini tiada sesi kerja baru yang direkodkan — last active session ialah 2026-08-04 (EA New v3 MinIO integration). Auto-diary EOD dijalankan untuk tutup hari secara bersih.

## Kerja Dilakukan
- Auto-diary EOD dijalankan oleh scheduler (tiada sesi manual hari ini)
- Context terakhir: EA New v3 — login redesign, fix bug Seksyen dropdown, MinIO custom client (SigV4)

## Keputusan
Tiada keputusan baru hari ini. Keputusan terbuka dari sesi lepas:
- MinIO: custom `MinioClient` (SigV4 manual), object key `ea_newv3/{no_aduan}/foto{n}.ext`
- Fallback wajib: MinIO down → local `uploads/wr/`
- CA bundle fix dalam kod (`applyCaBundle()`), bukan ubah `php.ini` server

## Open
- Test borang sebenar dalam browser — TERGANTUNG (perlu Abam pasang Chrome extension Claude)
- Sahkan reachability MinIO dengan IT dept secara rasmi
- Prod `.env` berasingan belum disediakan
- Retention/cleanup policy foto orphan — fasa 2

## Next Steps
1. Abam pasang/connect Claude Chrome extension → test MinIO upload guna borang sebenar
2. Prepare prod `.env` dengan credential sebenar (berasingan dari dev)
3. Confirm dengan IT dept reachability MinIO rasmi sebelum deploy
